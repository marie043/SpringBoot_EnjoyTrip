<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.enjoy.map.model.mapper.MapMapper">
	<select id="readSido" resultMap="myMap">
		select * from table
	</select>
	<select id="readGugun" resultMap="myMap">
		select * from table
	</select>
	<select id="readMap" parameterType="map" resultMap="myMap"></select>
</mapper>